import React from 'react';
import { StyleSheet, Text, View } from 'react-native';

const TopHeadlineComponent = () => {
  return (
    <View>
      <Text>TopHeadlineComponent</Text>
    </View>
  );
};

export default TopHeadlineComponent;

const styles = StyleSheet.create({});
